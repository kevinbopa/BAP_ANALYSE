psycopg[binary]>=3.2,<3.4
# Optional ML stack — used by train_xgboost.py and the runtime XGBoost predictor.
# The runtime engine works without these (3-way Poisson + Elo + Market blend).
xgboost>=2.0,<3.0
scikit-learn>=1.4,<2.0
joblib>=1.3,<2.0
numpy>=1.26,<3.0
